server:
  port: 5020

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/AMOOZESH
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true


